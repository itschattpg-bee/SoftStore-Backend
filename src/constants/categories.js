// Single source of truth for app categories, shown as a horizontal
// scroll of chips under the search bar on the home screen, and as the
// dropdown options in the upload form. Keep this list in sync with
// `kAppCategories` in the Flutter app's lib/config.dart.
const CATEGORIES = [
  "E-Commerce",
  "Social & Communication",
  "FinTech",
  "EdTech",
  "Healthcare",
  "Business & Productivity",
];

module.exports = { CATEGORIES };
